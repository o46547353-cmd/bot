# Telegram
python-telegram-bot[job-queue]==21.10

# Scheduler
apscheduler>=3.10.0

# HTTP — совместимые версии для PTB + metathreads
httpx>=0.27,<0.29
h11>=0.14.0
httpcore>=1.0.0,<2.0.0

# Threads API (metathreads — лайки, фолловы, реплаи, поиск, статистика)
metathreads

# Instagram auth + image posting (заменяет Danie1/threads-api)
instagrapi>=2.1.2

# TOTP — автогенерация 2FA кодов из Google Authenticator seed
pyotp>=2.9.0

# AI
openai>=1.50.0

# Utils
python-dotenv
requests
cryptography
pytz

# metathreads dependencies
beautifulsoup4
lxml
