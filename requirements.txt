# Telegram (тянет httpx + httpcore нужных версий сам)
python-telegram-bot[job-queue]==21.10

# Scheduler
apscheduler>=3.10.0

# Instagram auth ONLY (login, 2FA, TOTP, session save/restore)
instagrapi>=2.1.2

# TOTP — автогенерация 2FA кодов
pyotp>=2.9.0

# AI
openai>=1.50.0

# Utils
python-dotenv
requests
cryptography
pytz

# ─── УДАЛЕНО ───
# metathreads          — заменён на slash_threads_client.py
# beautifulsoup4, lxml — были зависимостями metathreads
