# Telegram
python-telegram-bot[job-queue]==21.10

# Scheduler [BUG-11 FIX: was missing entirely]
apscheduler>=3.10.0

# HTTP — совместимые версии для PTB + metathreads
httpx>=0.27,<0.29
h11>=0.14.0
httpcore>=1.0.0,<2.0.0

# Second Threads library: Danie1/threads-api [BUG-12 FIX]
threads-api==1.1.1

# aiohttp (required by threads-api) [BUG-13 FIX]
aiohttp>=3.8.0

# AI
openai>=1.50.0

# Utils
python-dotenv
requests
cryptography
pytz

# metathreads dependencies (ставим сами чтобы не было конфликтов)
beautifulsoup4
lxml
